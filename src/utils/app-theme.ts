import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#39CDCC",
    },
    secondary: {
      main: "#213F7D",
    },
    error: {
      main: "#E4033B",
    },
  },
});

export default theme;
